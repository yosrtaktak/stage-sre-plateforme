# =============================================================================
#  .trivyignore — les findings ASSUMÉS, jamais supprimés en silence.
#  Règle : une ligne = un ID + un commentaire DATÉ qui justifie (c'est
#  l'ancêtre CI du document VEX). Revue à chaque trimestre.
#  Destination : RACINE du repo (.trivyignore)
#
#  Exemple de format :
#  # 2026-08-11 — NodePort Grafana assumé : lab mono-utilisateur derrière NAT,
#  # sera fermé avec l'AuthorizationPolicy Istio (issue #NN)
#  AVD-KSV-0037
# =============================================================================
